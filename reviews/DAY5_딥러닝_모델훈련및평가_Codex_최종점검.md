# DAY5 딥러닝 모델 훈련 및 평가 - Codex 최종 점검

## 게시 가능

- PDF 58쪽의 핵심 내용과 통합 검토안의 높은 우선순위 수정사항이 대부분 반영됨
- validation과 test 역할, 데이터 누수 방지 원칙이 올바르게 설명됨
- Keras MNIST의 raw logits, 정수 라벨, `SparseCategoricalCrossentropy(from_logits=True)` 조합이 올바름
- tensor shape, dtype, CPU/GPU 장치 설명이 적절함
- Python 코드 블록 8개의 문법이 정상임
- NumPy 경사하강법ㆍ이동평균ㆍ필터 예제의 출력값을 재검증함
- Markdown 제목, 표, 링크, 코드 fence와 UTF-8 상태가 정상임
- TensorFlow가 현재 환경에 설치되지 않아 Keras 코드는 실제 실행하지 못하고 정적으로 검토함

## 게시 전 반드시 수정

1. 최종본 92행과 108행
   - `손실을 미분했을 때 0이 되는 지점`과 `기울기가 0 근처이면 최적점에 가까운 상태`라는 표현이 뒤의 지역 최솟값ㆍ극댓값ㆍ안장점 설명과 충돌함.
   - 다음처럼 수정해야 함.

```markdown
단순한 1차원 볼록 함수에서는 미분값이 0인 지점이 최솟값입니다. 그러나 실제 신경망에서는 기울기가 0에 가까운 지점이 정지점 후보일 뿐이며, 전역 최솟값인지는 보장되지 않습니다.
```

2. 최종본 464행
   - `합성곱을 하면 출력이 입력보다 작아집니다`는 padding과 stride 조건에 따라 틀릴 수 있음.
   - 다음처럼 수정해야 함.

```markdown
패딩 없이(`P=0`) stride 1로 합성곱하면, N개 데이터에 크기 K인 커널을 적용한 출력은 `N-K+1`개로 줄어듭니다.
```

## 선택적으로 개선

- 264행과 270행의 `현재 공식 권장 방식`을 `현재 TensorFlow 공식 beginner quickstart 방식`으로 완화 권장
- Cross Entropy를 단순한 `예측 확률과 실제 정답 확률의 차이`보다 `정답 클래스에 부여한 확률을 평가하는 손실`로 설명하면 더 정확함
- 전체 분량이 604줄이므로 최종 요약의 CNN 세부 항목을 조금 줄여도 됨

위 두 필수 문장을 수정하면 **게시 가능**함.
