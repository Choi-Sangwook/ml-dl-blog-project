<!--
============================================================
기획 문서 (드래프트 메타) — 게시 전 이 주석 블록은 제거 예정
============================================================

[작업 모드] Generate (Stage 1: 초안)
[1차 출처] sources/DAY5_딥러닝_모델훈련및평가.pdf (총 58페이지)
[분야] 딥러닝 / [독자] 파이썬 기본은 알지만 딥러닝은 처음
[프레임워크 결정]
  - 이번 PDF의 3장이 "TensorFlow 소개"이고, 유일한 완성형 코드 예제(p20)가
    Keras 15줄 MNIST임. → MNIST 완성 예제는 PDF에 충실하게 Keras로 작성.
  - 이전 DAY2·DAY4는 PyTorch였음 → 이 글에서 "프레임워크가 바뀐다"는 점을 명시(딥러닝 체크리스트 1).
  - CNN 합성곱/경사하강법 직관 예제는 NumPy(프레임워크 무관)로 작성하고 실제 실행값을 사용.

------------------------------------------------------------
1) PDF 페이지 → 핵심 주제 지도 (전 페이지 확인 완료)
------------------------------------------------------------
p1-2   표지/목차 (4장: ①모델 훈련·평가 ②기울기·미분·경사하강법 ③TensorFlow 소개 ④CNN 모델 분석)
p3     [1장] 훈련 단계: 데이터 입력 → 순전파(뉴런 계산 z, 활성화)
p4     활성화 함수 5종(Sigmoid/Tanh/ReLU/Leaky/Softmax), 예측값(확률표), 손실(회귀=MSE)
p5     손실(분류=Cross Entropy), 역전파(연쇄법칙), 가중치 갱신(경사하강), 학습률, Epoch
p6     Batch, 최적화 알고리즘(SGD/Momentum/AdaGrad/RMSProp/Adam/AdamW), 평가 도입, 데이터 분할(Train/Val/Test)
p7     분할 비율(70/15/15), 분류 지표: 정확도/정밀도/재현율/F1
p8     회귀 지표: MAE/RMSE/R², 과적합·과소적합과 대응(증강/Dropout/BN/EarlyStopping/L1L2)
p9-16  [2장] 기울기·미분·경사하강법: 오차 최소화, 가중치 무수히 많음→한번에 못 구함→반복 개선,
       기울기 부호로 이동 방향, 미분=0 지점, 학습률 중요성, 반복 학습(Epoch) 7단계
p17    [3장] 3대 프레임워크(Keras/TensorFlow/PyTorch) — 모두 파이썬 기반, 인기 추이 그래프
p18    TensorFlow 소개(구글, V1 2015/V2 2019, TPU 연동)
p19    Keras 소개(François Chollet 2015, TF를 쉽게)
p20    Keras 15줄 MNIST 코드(텐서플로 공식 튜토리얼) — 완성형 예제
p21-22 [4장] 합성곱의 직관: 이동평균(단순/가중) → "합성곱(Convolution) 연산"
p23    커널(Kernel)·계수(Weight) 정의, 1D 합성곱 식 y[n]=Σ h[k]x[n+k]
p24-25 필터링: 저역통과(LPF, 평탄화)·고역통과(HPF, [-1,2,-1] 엣지)
p26-27 영상=픽셀, 흑백(밝기값)·컬러(RGB), 해상도(Full HD 1920×1080)
p28    영상에서의 2D 합성곱(입력·커널 2차원)
p29-32 영상 필터: 평균필터(blur)·가우시안, 엣지 검출(Prewitt/Sobel), 포토샵 블러 예시
p33    컬러 합성곱(채널별 커널), 데이터 개수 줄어듦: N개 → N-K+1개
p34    해결법: Padding / Copying / Warping
p35    [4장-3] CNN 구조(Conv 층 반복), 합성곱 기반 연산
p36-39 첫 번째 층: RGB 채널별 계수, 몇 개의 커널?(한 커널=한 엣지검출), 커널 개수 부족 문제
p40    N 출력 Feature Maps(=채널 N개)
p41-43 다음 층 합성곱(커널 깊이=입력 채널 N), 합성곱층 일반화(M 출력), 6중 반복문, 곱셈 횟수
p44    곱셈 횟수 = M×R×C×N×K×K (예: 3.6×10⁹), 3D 커널(K×K×입력채널)
p45-46 완전 연결층(FC): 3D feature map → 1D vector → 일반 신경망층(1~3개)
p47-48 활성화 함수: 과거 Sigmoid/Tanh(기울기 소실) → 현재 ReLU/Leaky ReLU
p49-50 Pooling 층(MAX/AVG, 채널별), Stride(=2면 2칸씩, R=H/2)
p51-52 출력 크기: R=H-K+1, Padding(R=H+2P-K+1, P=(K-1)/2면 크기 유지)
p53    Stride 일반식 R=⌊(H-K)/S⌋+1, 합성곱층의 Bias(채널마다 동일 b 더함)
p54-57 배치 정규화: 공변량 쉬프트, 정규화(평균0/표준편차1), 미니배치, Scale/Shift(γ,β), 효과
p58    요약 슬라이드

[실제 실행으로 확인한 값만 사용 — 임의 생성 금지]
  - 경사하강법 f(w)=(w-3)²: step0~5 궤적, lr=0.01(느림)·0.1(수렴)·1.01(발산) → NumPy 실행 확인
  - 이동평균(w=3): [26.67, 33.33, 30, 20, 30, 30]
  - LPF 커널[1/4,1/2,1/4]: [25, 37.5, 30, 17.5, 27.5, 37.5]  (PDF p22와 일치)
  - HPF 커널[-1,2,-1] on step: 엣지에서 ±10, 평탄부 0  (PDF p25 "높은 주파수 없음"과 일치)
  - 출력 크기: valid(N=7,K=3)=5, pool(K=S=2,H=10)=5, same(K=3,P=1)=10
  - 곱셈 횟수 M=N=100,R=C=200,K=3 → 3,600,000,000 = 3.6×10⁹  (PDF p44와 일치)
  - Keras MNIST(p20): TensorFlow 미설치로 이 환경에서 실행 못함 → 정확도 수치 임의 기입 금지, 독자 실행 안내.

------------------------------------------------------------
2) 재구성(집필) 순서 — 입문자 기준
------------------------------------------------------------
PDF는 (훈련/평가)→(경사하강법)→(프레임워크)→(CNN) 순서지만,
입문자에게는 "학습이 무엇인가"를 먼저 직관→수학→평가→도구→CNN 순으로 재배열.
2. 딥러닝 학습의 큰 그림(순전파~역전파~갱신, epoch/batch, 옵티마이저)
3. 경사하강법과 기울기 (왜 반복하나 + NumPy 실습, 학습률)
4. 모델 평가 (분할, 분류·회귀 지표, 과적합/과소적합)
5. 프레임워크와 첫 코드 (TF/Keras/PyTorch, Keras MNIST)  ← 프레임워크 전환 명시
6. CNN (합성곱 직관+NumPy, 필터, 영상 합성곱, 출력 크기, CNN 구조, Pooling/활성화/Bias/BN)
7. 자주 하는 실수 / 8. 핵심 정리
============================================================
-->

# 🧠 딥러닝 완전 입문 가이드 — DAY5. 딥러닝 모델 훈련 및 평가

> **시리즈**: 파이썬 기본만 있는 사람을 위한 딥러닝 입문
> **이전 편**: DAY1(개념·설치) · DAY2(신경망 알고리즘·계층구조) · DAY3(데이터 처리·데이터셋) · DAY4(네트워크 모델 설계)
> **프레임워크 안내**: 이번 글의 **완성형 MNIST 예제만 Keras(TensorFlow)** 입니다. 이번 DAY의 강의 PDF가 TensorFlow/Keras를 소개하기 때문입니다. (DAY2·DAY4는 PyTorch였습니다.) 합성곱·경사하강법 **직관 실습은 NumPy**로 작성했고, 그 출력값은 **직접 실행해 확인한 값**입니다.

> 💡 **이 글의 표기 약속**
> - 🟦 **(강의 PDF)** : 강의 자료에 직접 나온 내용
> - 🟩 **(보충)** : 입문자를 위해 덧붙인 사전지식·실무 주의점
> - 코드 출력 중 **NumPy 예제(경사하강법·합성곱)** 는 실제 실행해 확인한 값입니다. **Keras MNIST 예제는 이 글을 쓴 환경에 TensorFlow가 없어 실행하지 못했습니다.** 정확도 수치를 임의로 적지 않았으니 직접 실행해 확인하세요.

---

## 1. 이번 DAY에서 배우는 것

지금까지는 신경망을 "어떻게 설계하는가"를 봤습니다. 이번 DAY는 설계한 모델을 **실제로 학습시키고(훈련), 잘 됐는지 확인하는(평가)** 전체 과정을 다룹니다.

- 딥러닝이 학습하는 한 사이클: **순전파 → 손실 계산 → 역전파 → 가중치 갱신**
- 학습의 수학적 심장인 **경사하강법(Gradient Descent)** 과 **학습률(Learning Rate)** — NumPy로 직접 확인
- 모델을 평가하는 법: **Train/Validation/Test 분할**, 분류 지표(정확도·정밀도·재현율·F1), 회귀 지표(MAE·RMSE·R²), **과적합/과소적합**
- 딥러닝 3대 프레임워크와 **Keras 15줄 MNIST** 첫 코드
- **CNN(합성곱 신경망)**: 합성곱이 무엇이고(이동평균에서 출발), 영상에서 어떻게 동작하며, 출력 크기·Padding·Stride·Pooling·Bias·배치 정규화까지

> 🟩 **(보충)** 이번 PDF는 폭이 넓습니다. 한 번에 모두 외우기보다, **"학습 = 손실을 줄이는 반복"** 이라는 큰 줄기를 먼저 잡고, CNN은 "이미지를 위한 특별한 층"으로 이해하는 데 집중하세요.

---

## 2. 딥러닝 학습의 큰 그림 🟦

딥러닝 모델 훈련(Training)이란 **신경망이 데이터로부터 규칙과 특징을 스스로 학습하는 과정**입니다. 예를 들어 고양이·강아지 사진을 구분하려면, 수많은 이미지와 정답(라벨)을 모델에 주고 각 동물의 특징을 학습시킵니다.

한 번의 학습 사이클은 다음 순서로 진행됩니다.

```text
①데이터 입력 → ②순전파(예측) → ③손실 계산 → ④역전파 → ⑤가중치 갱신 → (다시 ①)
```

### 2.1 순전파(Forward Propagation) — 예측 만들기

입력이 신경망의 각 층을 **앞쪽으로** 차례로 통과합니다. 각 뉴런은 입력에 가중치를 곱하고 편향을 더한 값 $z$ 를 계산한 뒤, **활성화 함수**를 통과시켜 다음 층으로 보냅니다.

$$
z = \sum_{i=1}^{n} w_i x_i + b
$$

대표적인 활성화 함수는 **Sigmoid, Tanh, ReLU, Leaky ReLU, Softmax** 입니다(자세한 비교는 6장 CNN 부분에서 다시 봅니다).

모든 층을 통과하면 최종 출력이 나옵니다. 예를 들어 손글씨 숫자(0~9) 분류 모델은 다음처럼 **각 숫자일 확률**을 출력합니다. 🟦

| 숫자 | 0 | 1 | 2 | 3 | … |
|---|---|---|---|---|---|
| 확률 | 0.01 | 0.02 | 0.03 | **0.91** | … |

이 경우 가장 확률이 높은 **숫자 3** 으로 예측합니다.

### 2.2 손실(Loss) — 얼마나 틀렸는가

예측과 실제 정답의 차이를 수치로 만든 것이 **손실 함수(Loss Function)** 입니다. **손실이 작을수록 좋은 모델**입니다. 🟦

- **회귀 문제 → 평균제곱오차(MSE)**: 실제 값과 예측 값의 차이를 제곱해 평균
  $$\text{MSE} = \frac{1}{n}\sum_{i=1}^{n}(y_i - \hat{y}_i)^2$$
- **분류 문제 → 교차 엔트로피(Cross Entropy)**: 예측 확률과 실제 정답 확률의 차이를 측정

> 🟩 **(보충)** "어떤 문제에 어떤 출력층·라벨·손실을 짝지어야 하는지"는 DAY4에서 자세히 다뤘습니다. 핵심만 다시 적으면: **회귀=MSE/MAE**, **이진분류=이진 교차 엔트로피**, **다중분류=교차 엔트로피**.

### 2.3 역전파(Backpropagation)와 가중치 갱신

손실이 나오면, 모델은 **"각 가중치가 이 오차에 얼마나 기여했는지"** 를 출력층에서 입력층 방향으로(=뒤로) 계산합니다. 이때 미분과 **연쇄법칙(Chain Rule)** 이 쓰입니다. 이렇게 구한 값이 **기울기(Gradient)** 이고, 이 기울기를 이용해 가중치를 조금씩 수정합니다(경사하강법, 3장에서 자세히).

$$
w \leftarrow w - \eta \cdot \frac{\partial L}{\partial w}
$$

여기서 $\eta$(에타)가 **학습률**입니다. **너무 크면 학습이 불안정하고, 너무 작으면 매우 느려집니다.**

### 2.4 Epoch, Batch, 그리고 옵티마이저

- **Epoch(에폭)**: 전체 학습 데이터를 한 번 모두 사용한 것. 데이터 10,000개를 모두 쓰면 1 Epoch. 🟦
- **Batch(배치)**: 데이터를 한 번에 다 처리하기 어려우므로 작은 묶음으로 나눠 학습. 데이터 10,000개를 **Batch Size 100** 으로 나누면, **100번 반복하면 1 Epoch** 가 끝납니다. 🟦
- **최적화 알고리즘(Optimizer)**: 가중치를 갱신하는 방법. **SGD, Momentum, AdaGrad, RMSProp, Adam, AdamW** 등이 있고, PDF는 *"현재 실무에서는 Adam·AdamW가 가장 널리 쓰인다"* 고 소개합니다. 🟦

> 🟩 **(보충)** **Epoch / Batch / 갱신 1회(step)** 는 다릅니다. 한 step은 배치 하나로 가중치를 한 번 갱신하는 것이고, 여러 step이 모여 1 Epoch가 됩니다. *"배치가 크면 무조건 빠르고 좋다"* 는 말은 사실이 아닙니다 — 메모리·일반화 성능과 균형을 봐야 합니다.

---

## 3. 경사하강법과 기울기 🟦

> 학습의 수학적 핵심입니다. PDF 2장 전체(p9~16)가 이 한 가지를 설명합니다.

### 3.1 왜 "한 번에" 정답을 못 구할까

딥러닝의 목표는 **예측 오차(Loss)를 최소화하는 가중치**를 찾는 것입니다. 수학적으로는 "손실을 가중치로 미분했을 때 0이 되는 지점"을 찾으면 됩니다. 하지만 실제 모델은 가중치가 **수백만~수천억 개** 입니다(예: 이미지 분류 CNN, 자연어 Transformer, GPT 같은 대규모 언어모델). 이걸 한 번에 정확히 푸는 것은 사실상 불가능합니다.

그래서 딥러닝은 정답을 한 번에 구하는 대신 **현재 위치에서 조금씩 더 나은 방향으로 반복해서 이동**합니다.

### 3.2 직관: 안개 낀 산에서 내려오기

경사하강법은 *"산에서 가장 낮은 계곡으로 내려가는 것"* 에 비유됩니다. 🟦

> ① 현재 위치 확인 → ② 주변 경사 확인 → ③ 가장 낮아지는 방향 선택 → ④ 조금 이동 → ⑤ 다시 경사 계산 → (반복)

여기서 **기울기(Gradient)** 는 현재 지점에서 손실 곡선이 얼마나 가파른지를 나타냅니다.

- 기울기가 **양수** → 왼쪽(값을 줄이는 쪽)으로 이동
- 기울기가 **음수** → 오른쪽으로 이동
- 기울기가 **0 에 가까움** → 최적점 근처

### 3.3 Python 실습 — 손실 곡선을 직접 내려가 보기 🟩

가장 단순한 손실 $f(w) = (w-3)^2$ 을 생각해 봅시다. 정답은 누가 봐도 $w=3$ 이지만, 모델은 그걸 모른 채 **기울기만으로** 찾아가야 합니다. 미분하면 $f'(w) = 2(w-3)$ 입니다.

```python
# 경사하강법 직관 실습 (NumPy / 프레임워크 무관) - 실제 실행 확인됨
import numpy as np

def f(w):    return (w - 3) ** 2     # 손실 함수
def grad(w): return 2 * (w - 3)      # 손실의 기울기(미분)

w  = 0.0      # 시작 가중치 (아무 값)
lr = 0.1      # 학습률 (한 번에 얼마나 이동할지)

print("step    w       loss     grad")
for step in range(6):
    print(f"{step:>3}  {w:7.4f}  {f(w):7.4f}  {grad(w):7.4f}")
    w = w - lr * grad(w)             # 핵심: 기울기 반대 방향으로 이동
```

**실행 결과(이 환경에서 확인):**

```text
step    w       loss     grad
  0   0.0000   9.0000  -6.0000
  1   0.6000   5.7600  -4.8000
  2   1.0800   3.6864  -3.8400
  3   1.4640   2.3593  -3.0720
  4   1.7712   1.5099  -2.4576
  5   2.0170   0.9664  -1.9661
```

**해석**: $w$ 가 0 → 0.6 → 1.08 …처럼 **정답 3을 향해** 이동하고, 그때마다 손실(9 → 5.76 → 3.69 …)이 줄어듭니다. 50번 반복하면 $w \approx 3.0$, 손실 $\approx 0$ 에 도달합니다. 이것이 학습의 전부입니다 — **기울기를 보고, 손실이 줄어드는 방향으로 조금씩 이동.**

### 3.4 학습률(Learning Rate)이 왜 그렇게 중요한가 🟦

같은 문제를 학습률만 바꿔 50번 반복한 결과입니다.

```python
def run(lr, steps=50):
    w = 0.0
    for _ in range(steps):
        w = w - lr * grad(w)
    return w, f(w)

for lr in [0.01, 0.1, 1.01]:
    w, loss = run(lr)
    print(f"lr={lr:<5}  ->  w={w:8.4f},  loss={loss:.4f}")
```

**실행 결과(이 환경에서 확인):**

```text
lr=0.01   ->  w=  1.9075,  loss=1.1936
lr=0.1    ->  w=  3.0000,  loss=0.0000
lr=1.01   ->  w= -5.0748,  loss=65.2018
```

| 학습률 | 결과 | 의미 |
|---|---|---|
| 0.01 (너무 작음) | $w=1.91$, 아직 3에 못 감 | 학습이 너무 **느림** |
| 0.1 (적절) | $w=3.00$, 손실 0 | 잘 **수렴** |
| 1.01 (너무 큼) | $w=-5.07$, 손실 폭증 | 최적점을 지나쳐 **발산** |

> 🟩 **(보충)** PDF의 *"학습률이 크면 최적점을 지나치고, 작으면 오래 걸린다"* 는 말이 숫자로 그대로 나타났습니다. 실무에서는 보통 0.001 부근에서 시작해(특히 Adam) 조정합니다.

이 반복 과정(① 입력 → ② 예측 → ③ 손실 → ④ 미분 → ⑤ 기울기 → ⑥ 가중치 수정 → ⑦ 다시 예측)을 수천·수만 번 반복하는 것이 바로 학습입니다. 🟦

---

## 4. 모델 평가 🟦

학습이 끝나면 **학습에 쓰지 않은 데이터**로 실제 성능을 측정해야 합니다.

### 4.1 데이터 분할 — Train / Validation / Test

| 데이터 | 용도 |
|---|---|
| **Train Set** | 모델 학습 |
| **Validation Set** | 하이퍼파라미터 조정 |
| **Test Set** | 최종 성능 평가 |

PDF 예시는 **Train 70% / Validation 15% / Test 15%** 입니다. 🟦

> 🟩 **(보충)** 가장 중요한 원칙: **Test Set으로 하이퍼파라미터를 조정하면 안 됩니다.** 조정은 Validation으로 하고, Test는 "마지막에 딱 한 번" 보는 시험지처럼 다뤄야 점수를 신뢰할 수 있습니다. 분류에서 클래스 비율이 치우쳐 있으면 분할 시 **계층적 분할(stratify)** 을 고려하세요.

### 4.2 분류(Classification) 평가 지표

혼동행렬의 네 칸(TP·FP·FN·TN)에서 출발합니다.

| 지표 | 정의(말로) | 식 |
|---|---|---|
| 정확도(Accuracy) | 전체 예측 중 정답 비율 | $\dfrac{TP+TN}{TP+TN+FP+FN}$ |
| 정밀도(Precision) | 양성이라 **예측한 것 중** 실제 양성 | $\dfrac{TP}{TP+FP}$ |
| 재현율(Recall) | **실제 양성 중** 모델이 찾아낸 비율 | $\dfrac{TP}{TP+FN}$ |
| F1 Score | 정밀도·재현율의 조화평균 | $2\cdot\dfrac{P\cdot R}{P+R}$ |

PDF의 핵심 경고: **정확도는 이해하기 쉽지만, 데이터가 불균형하면 신뢰하기 어렵습니다.** 🟦 그래서 불균형 상황에서는 **F1 Score** 를 자주 씁니다.

> 🟩 **(보충)** 예를 들어 "정상 99% / 불량 1%" 데이터에서 무조건 정상이라고 찍어도 정확도는 99%지만, 불량을 하나도 못 잡으므로 재현율은 0입니다. 그래서 **정밀도·재현율·F1을 함께** 봐야 합니다.

### 4.3 회귀(Regression) 평가 지표 🟦

| 지표 | 의미 |
|---|---|
| **MAE** (평균절대오차) | 오차의 절댓값 평균. 단위가 직관적 |
| **RMSE** (평균제곱근오차) | 오차를 제곱→평균→제곱근. 큰 오차에 더 민감 |
| **결정계수(R²)** | 모델 설명력. **1에 가까울수록** 좋음 |

### 4.4 과적합(Overfitting)과 과소적합(Underfitting) 🟦

- **과적합**: 훈련 성능은 높은데 **새로운 데이터에서 성능이 낮은** 현상. 학습 데이터를 "외워버린" 상태.
- **과소적합**: 모델이 **충분히 학습하지 못한** 상태.

PDF가 제시하는 과적합 완화 기법: **데이터 증강(Data Augmentation), Dropout, Batch Normalization, Early Stopping, 정규화(L1·L2), 더 많은 데이터 확보.** 🟦

> 🟩 **(보충)** 어느 기법도 과적합을 *"완전히 없애지는"* 못합니다. 데이터·모델·설정에 따라 **줄여 줄 수 있다**고 이해하세요. 과적합/과소적합은 **훈련 곡선과 검증 곡선을 함께** 봐야 판단할 수 있습니다(훈련↓ 검증↑로 벌어지면 과적합 신호).

---

## 5. 프레임워크와 첫 코드 🟦

### 5.1 딥러닝 3대 프레임워크

PDF는 **Keras · TensorFlow · PyTorch** 를 *"모두 파이썬 기반"* 으로 소개합니다. 🟦

| 프레임워크 | 한 줄 소개 (PDF 기준) |
|---|---|
| **TensorFlow** | 구글 브레인팀 제작. V1 2015 / V2 2019 공개, TPU 연동. 대규모·기업형에 강함 |
| **Keras** | François Chollet가 2015 공개. **TensorFlow를 쉽게 쓰도록** 돕는 고수준 도구 |
| **PyTorch** | 연구·실험에서 인기. (DAY2·DAY4에서 사용) |

> 🟩 **(보충)** *"무엇이 제일 좋다"* 는 시점에 따라 바뀌는 주장입니다. 현재 Keras는 TensorFlow에 기본 포함된 고수준 API(`tf.keras`)로 자리잡았습니다. 이번 예제는 **PDF에 충실하게 Keras** 로 작성합니다. 같은 글 안에서 PyTorch와 섞지 않습니다.

### 5.2 Keras 15줄로 MNIST 손글씨 분류 (PDF p20) 🟦

PDF는 텐서플로 공식 튜토리얼의 **15줄짜리 MNIST 예제**를 보여줍니다. MNIST는 0~9 손글씨 숫자 이미지(28×28, 흑백)를 모은 표준 데이터셋입니다.

```python
# TensorFlow / Keras (PDF p20, 텐서플로 공식 quickstart 예제)
# 설치: pip install tensorflow
import tensorflow as tf

mnist = tf.keras.datasets.mnist
(x_train, y_train), (x_test, y_test) = mnist.load_data()

# 픽셀값 0~255 -> 0~1 로 정규화 (학습 안정화)
x_train, x_test = x_train / 255.0, x_test / 255.0

model = tf.keras.models.Sequential([
    tf.keras.layers.Flatten(input_shape=(28, 28)),   # 28x28 -> 784
    tf.keras.layers.Dense(128, activation='relu'),    # 은닉층
    tf.keras.layers.Dropout(0.2),                     # 과적합 완화
    tf.keras.layers.Dense(10, activation='softmax'),  # 0~9, 클래스 10개
])

model.compile(optimizer='adam',
              loss='sparse_categorical_crossentropy',  # 정수 라벨용
              metrics=['accuracy'])

model.fit(x_train, y_train, epochs=5)        # 학습
model.evaluate(x_test, y_test)               # 평가
```

**shape·라벨·손실 짝 맞추기(딥러닝 핵심 점검):**

| 항목 | 값 | 설명 |
|---|---|---|
| 입력 `x_train` | `(60000, 28, 28)` | 흑백 이미지 6만 장 |
| `Flatten` 후 | `(batch, 784)` | 28×28을 한 줄로 폄 |
| 출력층 | `Dense(10, softmax)` | 클래스 10개 확률 |
| 라벨 `y_train` | `(60000,)`, 정수 0~9 | 그래서 `sparse_categorical_crossentropy` |

> 🟩 **(보충)** **출력층이 softmax(확률)** 이므로 손실은 기본값(`from_logits=False`)인 `sparse_categorical_crossentropy` 와 짝이 맞습니다. **라벨이 정수**(원-핫이 아님)라서 `sparse_…` 를 씁니다. 원-핫 라벨이라면 `categorical_crossentropy` 를 씁니다.

> ⚠️ **결과값 안내**: 이 글을 작성한 환경에는 TensorFlow가 설치돼 있지 않아 **위 코드를 실행하지 못했습니다.** 그래서 정확도 수치를 임의로 적지 않았습니다. 직접 실행하면 `fit` 단계에서 Epoch마다 손실·정확도가 출력되고, `evaluate` 가 테스트 손실·정확도를 돌려줍니다. (이 예제는 텐서플로 공식 튜토리얼의 표준 예제입니다.)

---

## 6. CNN — 합성곱 신경망 🟦

PDF 4장 전체(p21~58)가 CNN입니다. CNN은 **영상(이미지) 처리에 주로 쓰는** 신경망으로, 핵심은 **합성곱(Convolution) 연산** 입니다. 큰 그림부터 잡고 갑니다.

```text
영상 → [합성곱층 1] → [합성곱층 2] → … → [합성곱층 N] → [완전연결층(FC) 1~3개] → 출력
```

### 6.1 합성곱은 "이동 평균"에서 출발한다 🟦

PDF는 합성곱을 **이동 평균(Moving Average)** 으로 설명합니다. 데이터를 따라가며 이웃한 값들의 (가중)평균을 구하는 것입니다. 데이터가 급격히 변할 때 **부드럽게(평탄하게)** 만들어 줍니다.

#### Python 실습 — 단순/가중 이동 평균 🟩

```python
# 합성곱 직관 실습 (NumPy) - 실제 실행 확인됨
import numpy as np

x = np.array([10, 20, 50, 30, 10, 20, 60, 10], dtype=float)

# (1) 단순 이동 평균: 이웃 3개의 평균 (커널 = [1/3, 1/3, 1/3])
simple = np.convolve(x, np.ones(3) / 3, mode='valid')
print("단순 이동평균 :", np.round(simple, 2))

# (2) 가중 이동 평균: 가운데에 더 큰 가중치 (커널 = [1/4, 1/2, 1/4])
y = np.array([0.25*x[i-1] + 0.5*x[i] + 0.25*x[i+1] for i in range(1, len(x)-1)])
print("가중 이동평균 :", y)
```

**실행 결과(이 환경에서 확인):**

```text
단순 이동평균 : [26.67 33.33 30.   20.   30.   30.  ]
가중 이동평균 : [25.  37.5 30.  17.5 27.5 37.5]
```

가중 이동평균 결과 `[25, 37.5, 30, 17.5, 27.5, 37.5]` 는 **PDF p22의 값과 정확히 일치**합니다. 여기서 곱해지는 수들의 묶음 `[1/4, 1/2, 1/4]` 을 **커널(Kernel)**, 그 안의 값들을 **계수(Weight)** 라고 부릅니다. 🟦

1차원 합성곱의 일반식은 다음과 같습니다. 🟦

$$
y[n] = \sum_{k=-\frac{K-1}{2}}^{\frac{K-1}{2}} h[k]\, x[n+k]
\qquad (h: \text{커널})
$$

### 6.2 필터링 — 저역통과(LPF)와 고역통과(HPF) 🟦

합성곱은 신호 처리에서 **필터링**에 많이 쓰입니다. 입력에서 내가 원하는 성분만 걸러내는 것입니다.

- **저역통과 필터(LPF)**: **천천히 변하는(낮은 주파수)** 성분만 통과 → 영상이 **부드러워짐(blur)**. 위의 `[1/4, 1/2, 1/4]` 가 LPF입니다.
- **고역통과 필터(HPF)**: **급격히 변하는(높은 주파수)** 성분만 통과 → **경계(엣지)** 만 남음. PDF 예시 커널은 `[-1, 2, -1]`.

#### Python 실습 — HPF로 경계 찾기 🟩

```python
# 고역통과 필터 실습 (NumPy) - 실제 실행 확인됨
import numpy as np

# 평평하다가 계단처럼 올라갔다 내려오는 신호
x = np.array([0, 0, 10, 10, 10, 10, 10, 10, 0, 0], dtype=float)
y = np.array([-1*x[i-1] + 2*x[i] - 1*x[i+1] for i in range(1, len(x)-1)])
print("HPF [-1,2,-1] :", y)
```

**실행 결과(이 환경에서 확인):**

```text
HPF [-1,2,-1] : [-10.  10.   0.   0.   0.   0.  10. -10.]
```

**해석**: 값이 **변하는 지점(경계)** 에서만 ±10이 나오고, **평평한 구간은 모두 0** 입니다. PDF p25의 *"높은 주파수 성분이 없음 → 평탄부는 사라지고 경계만 검출"* 이 숫자로 확인됩니다. 🟦

### 6.3 영상에서의 합성곱 🟦

- **영상은 픽셀(pixel)** 이라는 아주 작은 점들로 이루어집니다. **흑백 영상**은 픽셀마다 밝기 숫자를 가집니다(작으면 어두움, 크면 밝음). 🟦
- **컬러 영상**은 한 픽셀을 **R·G·B 세 값**으로 표현합니다. 이 셋을 조합해 하나의 색이 됩니다. 🟦
- **해상도(Resolution)** 는 한 사진의 픽셀 개수입니다(예: Full HD = 1920×1080). 🟦

영상에서의 합성곱은 1차원이 2차원으로 확장된 것입니다. **$K \times K$ 크기의 커널을 이미지 위에서 한 칸씩 미끄러뜨리며** 곱하고 더해 새 값을 만듭니다. 평균 필터는 흐림(blur), Prewitt·Sobel 커널은 경계 검출(edge)에 쓰입니다. 🟦

> 🟩 **(보충)** "포토샵 블러", "사진 보정으로 피부톤이 균일해짐" 같은 효과가 바로 저역통과 필터(합성곱)의 결과입니다 — CNN이 배우는 연산이 우리가 쓰는 이미지 필터와 같은 원리입니다.

### 6.4 출력 크기, Padding, Stride — 숫자로 정리 🟦

합성곱을 하면 **출력이 입력보다 작아집니다.** 데이터 N개에 크기 K인 커널을 valid하게 적용하면 출력은 **N − K + 1개**. (예: 7개 → 5개) 🟦

이걸 막거나 조절하는 두 장치가 **Padding** 과 **Stride** 입니다.

| 장치 | 무엇 | 출력 크기 공식 | 효과 |
|---|---|---|---|
| 없음(valid) | 그대로 | $R = H - K + 1$ | 가장자리만큼 줄어듦 |
| **Padding** | 가장자리에 값을 덧댐(0/복사/순환) | $R = H + 2P - K + 1$ | $P=\frac{K-1}{2}$ 면 **크기 유지** |
| **Stride** | 커널을 S칸씩 건너뜀 | $R = \left\lfloor \frac{H-K}{S} \right\rfloor + 1$ | S=2면 출력이 **절반(H/2)** |

PDF가 소개하는 Padding 방식: **Padding(0으로 채움) · Copying(가장자리 값 복사) · Warping(반대쪽 값으로 순환).** 🟦

#### Python 실습 — 출력 크기 공식 확인 🟩

```python
def out_size(H, K, P=0, S=1):
    return (H - K + 2*P) // S + 1

print("valid  N=7,  K=3        :", out_size(7, 3))          # 5   (N-K+1)
print("same   H=10, K=3, P=1   :", out_size(10, 3, P=1))    # 10  (크기 유지)
print("stride H=10, K=2, S=2   :", out_size(10, 2, S=2))    # 5   (H/2)
```

**실행 결과(이 환경에서 확인):**

```text
valid  N=7,  K=3        : 5
same   H=10, K=3, P=1   : 10
stride H=10, K=2, S=2   : 5
```

### 6.5 CNN의 구조 — 합성곱층, 채널, 그리고 연산량 🟦

- **첫 번째 합성곱층**: 입력(예: RGB 3채널)에 여러 개의 커널을 적용합니다. **커널 1개 = 한 종류의 특징(엣지 등) 검출기**. 커널이 너무 적으면 특징을 충분히 잡지 못합니다. 🟦
- **N개의 커널을 쓰면 N개의 출력 Feature Map(=채널 N개)** 이 나옵니다. 🟦
- **다음 층의 커널은 깊이가 입력 채널 수와 같습니다.** 즉 3×3 커널이라도 입력 채널이 N이면 실제로는 **3×3×N** 짜리 3차원 커널입니다. 🟦

#### 연산량은 얼마나 될까 — 곱셈 횟수 🟦

PDF는 한 합성곱층의 곱셈 횟수를 다음과 같이 제시합니다.

$$
\text{곱셈 횟수} = M \times R \times C \times N \times K \times K
$$

($M$: 출력 채널, $R \times C$: 출력 크기, $N$: 입력 채널, $K \times K$: 커널 크기)

```python
# PDF p44 예시 그대로 - 실제 실행 확인됨
M = N = 100      # 출력/입력 채널
R = C = 200      # 출력 크기
K = 3            # 커널
print(f"{M*R*C*N*K*K:,}")   # 3,600,000,000  = 3.6 x 10^9
```

**실행 결과**: `3,600,000,000` (= **3.6 × 10⁹**, PDF와 일치). 한 층에서만 수십억 번의 곱셈이 일어나므로 GPU가 필요합니다. 🟩

- **완전 연결층(Fully-Connected Layer, FC)**: 합성곱층들을 지난 뒤, **3차원 Feature Map을 1차원 벡터로 펴서** 일반 신경망층(보통 1~3개)에 넣어 최종 출력을 만듭니다. 🟦

### 6.6 활성화 함수 — 과거 vs 현재 🟦

활성화 함수는 합성곱층·FC층 **사이사이**에 들어가 비선형성을 줍니다.

| 시기 | 함수 | 특징 |
|---|---|---|
| 과거 | **Sigmoid** $\sigma(x)=\frac{1}{1+e^{-x}}$, **Tanh** | 양 끝에서 **미분값이 매우 작아 학습이 잘 안 됨**(기울기 소실) |
| 현재 | **ReLU** $\max(0,x)$ | 음수는 0, 양수는 그대로. 간단하고 빠름 |
| 현재 | **Leaky ReLU** $\max(0.1x,\,x)$ | 음수도 작은 값으로 통과(ReLU의 약점 보완) |

> 🟩 **(보충)** ReLU가 기울기 소실 문제를 *"완전히"* 없애는 것은 아닙니다(음수 입력이 계속되면 뉴런이 죽는 dead ReLU 위험). 다만 깊은 망에서 Sigmoid/Tanh보다 학습이 잘 되는 경우가 많아 은닉층 기본값으로 널리 씁니다. **출력층**의 활성화는 문제 유형에 맞춥니다(다중분류=softmax 등).

### 6.7 Pooling 층, Bias, 배치 정규화 🟦

- **Pooling 층**: 각 Feature Map(채널)마다 **MAX 또는 AVG** 연산을 적용해 크기를 줄입니다. 합성곱이 채널 방향으로 다 더하는 것과 달리, **Pooling은 채널마다 따로** 합니다. Stride와 함께 쓰면 출력이 줄어 계산량이 감소합니다. 🟦
- **Bias**: 한 출력 Feature Map의 모든 위치에 **같은 편향 $b$** 를 더합니다(채널마다 하나). 🟦
- **배치 정규화(Batch Normalization)**: 🟦
  - **문제(공변량 쉬프트)**: 학습이 진행되면 각 층의 계수가 바뀌고, 그러면 다음 층에 들어오는 **입력 분포가 계속 바뀝니다.** 층이 깊을수록 심합니다.
  - **해결**: 어떤 층의 출력을 **평균 0, 표준편차 1** 로 정규화합니다. 단, 미니배치 단위로 평균·표준편차를 구하고($\mu_B, \sigma_B$), 정규화 뒤 **Scale($\gamma$)·Shift($\beta$)** 를 학습해 되돌립니다: $y_i = \gamma \hat{x}_i + \beta$. 보통 **활성화 함수 직전**에 넣습니다.
  - **효과(PDF)**: 학습률을 크게 해도 학습이 잘 되고(속도 향상), 과적합이 줄며, 계수 초기화에 덜 민감해집니다. 🟦

> 🟩 **(보충)** 배치 정규화는 **층(layer)** 입니다. 프레임워크에서는 `BatchNorm`(PyTorch) / `BatchNormalization`(Keras) 한 줄로 추가합니다. "Dropout 같은 기법을 안 써도 될 정도로 과적합이 준다"는 PDF 표현은 *경향* 이지, 모든 경우 보장은 아닙니다.

---

## 7. 자주 하는 실수 🟩

- **정확도만 보고 좋아하기**: 불균형 데이터에서는 정밀도·재현율·F1을 함께 확인하세요.
- **Test Set으로 튜닝하기**: 하이퍼파라미터는 Validation으로 조정하고, Test는 마지막 한 번만.
- **학습률을 아무렇게나**: 너무 크면 발산(위 실습의 `lr=1.01`), 너무 작으면 학습이 안 끝납니다.
- **출력층·라벨·손실 짝 안 맞추기**: 예) softmax 출력엔 `from_logits=False` 손실, 정수 라벨엔 `sparse_categorical_crossentropy`. (DAY4 참고)
- **합성곱 출력 크기 착각**: 패딩이 없으면 매 층 크기가 $K-1$ 만큼 줄어듭니다. 깊은 망에서는 same 패딩으로 크기를 유지하는 경우가 많습니다.
- **프레임워크 섞기**: 한 예제 안에서 Keras와 PyTorch API를 섞지 마세요. 이 글의 MNIST는 Keras 한 가지입니다.

---

## 8. DAY5 핵심 정리

```text
딥러닝 학습 = "손실을 줄이는 반복"
  ├─ 한 사이클: 순전파(예측) → 손실 → 역전파 → 가중치 갱신
  ├─ 경사하강법: 기울기 반대 방향으로 조금씩 이동 (학습률이 속도·안정성 결정)
  └─ 단위: step(배치 1회) < Epoch(전체 1회)

평가
  ├─ 분할: Train(학습) / Validation(튜닝) / Test(최종, 1회)
  ├─ 분류: 정확도·정밀도·재현율·F1  (불균형이면 정확도만 믿지 말 것)
  ├─ 회귀: MAE·RMSE·R²(1에 가까울수록 좋음)
  └─ 과적합/과소적합: 훈련·검증 곡선을 함께 보기

프레임워크
  └─ TensorFlow/Keras/PyTorch — 이 글의 MNIST는 Keras(15줄)

CNN (이미지를 위한 신경망)
  ├─ 합성곱 = 커널을 미끄러뜨리며 곱·합 (이동평균/필터에서 출발)
  ├─ 필터: LPF(흐림) / HPF(엣지)
  ├─ 출력 크기: valid N-K+1 / same(P=(K-1)/2) 유지 / stride ⌊(H-K)/S⌋+1
  ├─ 채널: 커널 N개 → 출력 채널 N개, 다음 층 커널 깊이 = 입력 채널
  ├─ FC층: 3D feature map → 1D vector → 최종 출력
  ├─ 활성화: 은닉층 ReLU/Leaky ReLU (과거 Sigmoid/Tanh는 기울기 소실)
  └─ 안정화: Pooling(MAX/AVG), Bias(채널당 1개), 배치 정규화(분포를 평균0·표준편차1로)
```

---

## 참고 자료

- 강의 PDF: **DAY5. 딥러닝 모델 훈련 및 평가** (sources/DAY5_딥러닝_모델훈련및평가.pdf), 전 58페이지
- TensorFlow 2 quickstart (MNIST 예제): <https://www.tensorflow.org/tutorials/quickstart/beginner>
- 이전 편: DAY4 딥러닝 네트워크 모델 설계(출력층·라벨·손실 짝 맞추기), DAY2 신경망 알고리즘·계층 구조
